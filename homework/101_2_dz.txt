Перед выполнением заданий важно самостоятельно повторить шаги, выполняемые на уроке.

1) Выяснить какой загрузчик ОС использется в вашей системе.
ls /boot    # При загрузке также пишется если нажать 'shift'
2) Просмотреть журнал событий ядра.
dmesg                   # или less /var/log/dmesg если нужна информация о загрузке системы только
3) Изменить носитель по умолчанию, используемый для загрузки ОС.
mv /boot/grub/grub.cfg /boot/grub/grub_backup.cfg'; sed 's/set default\=\"0\"/set default\=\"4\"/g' /boot/grub/grub_backup.cfg' > /boot/grub/grub.cfg; reboot
4) Попробовать загрузить ОС в режиме, отличающемся от режима по умолчанию.
init 1    # Однопользовательский режим, в /etc/inittab прописаны режимы загрузки
5) Узнать какой из стилей инициализации используется в вашей ОС.
runlevel
